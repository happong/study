# 배운 개념 정리 (레벨별)

## Lv.1 print()
- `print()`로 문자열/숫자 출력
- 여러 줄 `print()`는 위에서부터 순차 실행됨 (자동으로 합쳐지지 않음)

## Lv.2 변수
- 변수 선언과 할당 (`x = 값`)

## Lv.3 자료형
- 문자열(str), 정수(int), 실수(float), 불리언(bool)

## Lv.4 input()
- `input()`으로 사용자 입력 받기 (기본적으로 문자열로 반환됨)

## Lv.5 if
- `if` / `elif` / `else`, 조건문 끝에는 콜론(`:`) 필수

## Lv.6 for
- `for` 반복문, `range()` 사용
- `print()`는 기본적으로 각 호출마다 자동 줄바꿈됨

## Lv.7 while
- `while` 반복문, 경계값 처리 (`<` vs `<=`)

## Lv.8 함수
- `def`로 함수 정의, 매개변수/인자
- `return`(값을 함수 밖으로 반환) vs `print()`(화면에 출력만 함) 구분

## Lv.9 리스트
- 인덱스로 요소 접근 (`list[i]`)
- `append()`로 요소 추가
- `len()`으로 길이 구하기
- `range(len(list))` 패턴으로 인덱스 순회
- 최댓값/최솟값 찾을 때 시작값 설정 주의

## Lv.10 딕셔너리 (도입: 2026-08-10, 100% 완료: 2026-09-02)
- 기본 키 접근: `dict[key]`
- 키 추가/수정: `dict[key] = value`
- `.items()`로 키-값 쌍 순회: `for key, value in dict.items():`
- `"key" in dict`로 키 존재 여부 확인
- `[]`(딕셔너리/리스트 접근) vs `()`(함수 호출) 구분
- `return True/False`(불리언) vs `return "True"/"False"`(문자열) 구분
- `.items()`(끝에 s) 정확한 철자, `return`은 반복문이 끝난 뒤에
- 키 이름 철자는 문제와 정확히 일치하는지 항상 확인 (우연히 출력이 맞아도 오타는 숨은 버그)

## Lv.11 튜플 (도입: 2026-09-07, 진행 중)
- 생성: `t = (1, 2, 3)` — 소괄호, 리스트(`[]`)와 다름
- 인덱스 접근: `t[0]` (리스트와 동일한 방식)
- 언패킹: `a, b, c = t`로 여러 변수에 한 번에 값 할당 (`for k, v in dict.items():`도 사실 튜플 언패킹)
- 불변성: 한 번 만들면 요소를 수정/추가/삭제할 수 없음 (`t[0] = 값` → `TypeError`)
- 🎉 2026-09-16 기준 복습 3연속 100% 달성, 완전히 체화됨

## Lv.12 파일 입출력 (도입: 2026-09-17, 진행 중)
- 파일 쓰기: `with open("파일명", "w") as f: f.write("내용")`
- 파일 읽기: `with open("파일명", "r") as f: content = f.read()`
- `with ... as f:` — 블록이 끝나면 파일을 자동으로 닫아줌
- `"w"`(쓰기 전용)와 `"r"`(읽기 전용)은 서로 다른 모드 — 쓰고 나서 읽으려면 별도로 다시 열어야 함

→ 재도전 세트 #2(80%)→#3(60%)→#4(90%)→#5(100%)로 통과. 자주 틀렸던 패턴은 `mistakes/wrong-patterns.md` 참고. Lv.11(튜플)은 사용자가 "다음 진도 배웠어"라고 말하면 시작.
